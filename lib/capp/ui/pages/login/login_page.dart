import 'package:flutter/material.dart';

class CLoginPage extends StatelessWidget {
  const CLoginPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("登陆"),
      ),
    );
  }
}
