import 'package:flutter/material.dart';

class FutureLoadingView extends StatelessWidget {
  const FutureLoadingView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("加载中"),
    );
  }
}
